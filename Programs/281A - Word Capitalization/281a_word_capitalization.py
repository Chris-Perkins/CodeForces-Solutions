# 281a_word_capitalization: capitalize a word (aka make the first character uppercase)
# not a stress on knowledge, but I need Python practice, so we start at the basics.

word = raw_input()

print word[:1].upper() + word[1:]